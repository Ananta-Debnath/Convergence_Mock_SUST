# Base URL the React app uses to reach the triage API.
# In dev, the Vite proxy strips `/api` and forwards to http://localhost:3000.
# In prod, point this at your deployed API origin (e.g. https://api.example.com/api).
VITE_API_BASE=/api